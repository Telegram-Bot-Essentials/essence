includes:
    - vendor/larastan/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    paths:
        - src
        - database
        - config
        - routes

    level: max

    tmpDir: build/phpstan
